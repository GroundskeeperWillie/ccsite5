<tr>
<td><%= link_to user.name, user %></td>
<td><%= user.guests.count %></td>
<% if current_user.admin? && !current_user?(user) %>
  <td>
    <%= link_to "remove", user, method: :delete,
									data: { confirm: "Are you sure?"}%>
	<% end %>
 </td>
</tr>